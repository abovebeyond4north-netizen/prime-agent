name: Agents view path cache bootstrap

on:
  push:
    branches:
      - perf/cache-agents-view-session-paths

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - name: Checkout branch
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          ref: perf/cache-agents-view-session-paths

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22

      - name: Apply focused backport
        run: python .github/scripts/cache_agents_view_session_paths_patch.py

      - name: Validate focused backport
        run: |
          npm ci --ignore-scripts
          npm run build
          npm run check
          npx vitest --run packages/coding-agent/test/agents-view-path-cache.test.ts

      - name: Commit validated patch
        run: |
          changed="$(git diff --name-only)"
          echo "$changed"
          for required in packages/coding-agent/src/modes/agents-view/agents-view-state.ts packages/coding-agent/test/agents-view-path-cache.test.ts packages/coding-agent/.changes/agents-view-session-path-cache.md; do
            echo "$changed" | grep -Fx "$required"
          done
          test "$(printf '%s\n' "$changed" | sed '/^$/d' | wc -l)" -eq 3
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add packages/coding-agent/src/modes/agents-view/agents-view-state.ts packages/coding-agent/test/agents-view-path-cache.test.ts packages/coding-agent/.changes/agents-view-session-path-cache.md
          git commit -m "perf: cache agents-view session paths"
          git push origin HEAD:perf/cache-agents-view-session-paths
